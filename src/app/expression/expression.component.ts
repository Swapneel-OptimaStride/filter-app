import { Component, Input, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'app-expression',
  templateUrl: './expression.component.html',
  standalone: true,
  imports: [],
})
 export class ExpressionComponent {
//   @Input() categories: any;
//   @Input() opList: any;
  // @Input() section: any;
//   @Input() index!: number;


//   @Output() onTypeChange = new EventEmitter<string>();
//   @Output() onCategoryChange = new EventEmitter<any>();
//   @Output() onOperationChange = new EventEmitter<any>();

  
//   formSections = [{ value: '' }];  // Example initialization, adjust as needed

//   // Method to handle category change
//   onCategoryChangeHandler(value: any) {
//     this.onCategoryChange.emit(value);
//   }

//   // Method to handle operation change
//   onOperationChangeHandler(value: any) {
//     this.onOperationChange.emit(value);
//   }

//   // Method to handle type change
//   onTypeChangeHandler(value: string) {
//     this.onTypeChange.emit(value);
//   }
 }
